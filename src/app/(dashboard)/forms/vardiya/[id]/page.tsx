// Vardiya Faz 1: detay by-id paylaşımlı — mesai detay sayfası formTipi-farkında
// (back-link/başlık form.formTipi'den). Aynı component tek kaynak.
export { default } from "../../overtime/[id]/page"
